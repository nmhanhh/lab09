<?php
    $host = "https://feenix-mariadb-web.swin.edu.au";         // because XAMPP runs the server locally
    $user = "s104221122";          // default username for XAMPP's MySQL
    $pwd = "280503";              // default password is empty in XAMPP
    $sql_db = "s104221122_db";  // replace with the actual name of your database

    // $conn = mysqli_connect($host, $username, $password, $database);
    // if (!$conn) {
    //     die("Connection failed: " . mysqli_connect_error());
    // }
?>